service: todo
useDotenv: true

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-south-1
  stage: dev

functions:
  app:
    handler: handler.handler
    name: todo
    description: API Gateway for todo
    memorySize: 128
    timeout: 10
    events:
      - httpApi: "*"